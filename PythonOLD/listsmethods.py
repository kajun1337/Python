numbers = [1,10,5,16,4,9,10]
letters = ["a","b","c","d","e"]
"""
val = min(numbers)  #dizi içindeki minimum değeri alırız.

val = max(numbers)  #dizi içindeki max değeri alırız.

val = max(letters) #dizi içindeki alfabetik olarak max değeri alırız.

val = min(letters) #dizi içindeki alfabetik olarak min değeri alırız.

val=numbers[3:6] #diziyi 3ten 6 ya kadar alırız.

numbers [4]=5 # dizi elemanini değiştirdik.

numbers.append(49) # dizinin sonuna yeni eleman eklemek için kullandık.

numbers.insert(3,78) # dizide ilk parametrede seçilen yerin öncesine ikinci parametredeki elemanı yazar.

numbers.insert(-1,33) # dizide ilk parametrede seçilen yerin öncesine ikinci parametredeki elemanı yazar.

numbers.pop() # dizideki son elemanı sileriz. Parametre olarak eleman girildiğinde o elamanı sileriz.

letters.remove("a") # dizideki parametrede verilen elemanı siler

numbers.sort() # diziyi sayısal olarak sıralarız.

numbers.reverse() #diziyi ters çevirir.

numbers=len(numbers) # dizinin boyutunu bulduk.

"""
numbers = numbers.count(10) # dizide parametredeki değerden kaç tane olduğunu gösterir.

letters.clear() # dizideki bütün elemanları sileriz.

print(letters)

